application:
  version: ${version}

server:
  port: 8080
  servlet:
    contextPath: /checkit-server

keycloak:
  realm: YourRealm
  clientId: CheckIt-server
  authUrl: http://localhost/auth
  realmKey: YourKey
  secret: YourSecret

repository:
  url: http://localhost/repo
  driver: cz.cvut.kbss.ontodriver.sesame.SesameDataSource
  language: cs
  userIdPrefix: https://slovník.gov.cz/uživatel/

